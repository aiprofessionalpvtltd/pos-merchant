# Provider fixtures

Real eDahab and WaafiPay responses captured from the provider APIs (API keys
removed). Tests load them with `fixture('edahab/check-invoice-pending.json')`.

Only add responses captured from a real call. See `docs/edahab.md` and
`docs/waafipay.md` for the list still to capture.
